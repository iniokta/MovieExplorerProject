import React from 'react';
import { View, Text } from 'react-native';

const MovieCard = () => {
  return (
    <View>
      <Text>Movie Card Test</Text>
    </View>
  );
};

export default MovieCard;
